# The cluster identity Secret is created and managed automatically by the operator at runtime.
# The operator calls ReattachCluster on first startup, receives a backend-assigned UUID,
# and writes it into this Secret (name configured via clusterIdentitySecretName in values.yaml).
# Helm does not pre-create this Secret — the operator owns it entirely.